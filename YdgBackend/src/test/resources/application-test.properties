spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Datasource is provided by Testcontainers in integration tests
# For unit/web tests without DB interaction, no datasource is required

# Expose actuator endpoints for e2e checks if running locally
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
